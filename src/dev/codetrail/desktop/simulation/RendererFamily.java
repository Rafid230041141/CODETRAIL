package dev.codetrail.desktop.simulation;

/** The six renderer families shared by the DSA simulations. */
public enum RendererFamily {
    ARRAY,
    LINKED,
    TREE,
    GRAPH,
    TABLE,
    STACK
}
